{% extends 'dashboard/layout.html.twig' %}

{% block panel_heading %}Detalles de cliente{% endblock %}
{% block panel_body %}
    {{ include('form_field.html.twig', { 'label': 'Cédula', 'tag':'input', 'value': client.ci }) }}
    {{ include('form_field.html.twig', { 'label': 'Nombre', 'tag':'input', 'value': client.name }) }}
    {{ form_start(delete_form) }}
        <a href="{{ path('client_index') }}" class="btn btn-default">Volver al listado</a>
        <a href="{{ path('client_edit', { 'id': client.id }) }}" class="btn btn-default">Modificar</a>
        <input type="submit" class="btn btn-default" value="Eliminar">
    {{ form_end(delete_form) }}
    </div>
{% endblock %}
